# Regression Surfaces

For a bounded L1 change, verify:

- the marker and repository rules remain readable;
- the static check still passes;
- Router assembly contains Demo Project context;
- no Runtime task history is added to a distribution package.
