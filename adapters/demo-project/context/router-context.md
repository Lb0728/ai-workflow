# Demo Project Router Context

This Adapter represents a small text project used only to prove that Core can
load a second project without project-specific code in Core.

- Prefer `light_feature` for a bounded document or static-check change.
- Escalate when release rules, destructive changes or migration are involved.
- Use `scripts/static_check.sh` as the declared L1 project check.
